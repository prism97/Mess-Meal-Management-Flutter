import 'package:flutter/material.dart';

class MealCheckScreen extends StatelessWidget {
  static String id = 'meal_check_screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lightBlueAccent,
      body: Container(),
    );
  }
}
